# Merge Sorted Arrays

TypeScript project to merge three sorted arrays into a single ascending sorted array.

## Project Setup

- Node.js v14+
- TypeScript
- Jest (testing framework)

## Setup

```bash
npm install
npm test
npm run build
```